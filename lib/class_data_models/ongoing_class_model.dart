class OngoingClassModel {
  final String courseName;
  final String courseCode;
  final DateTime startDate;
  final DateTime endDate;

  OngoingClassModel(this.courseName, this.courseCode, this.startDate, this.endDate);
}